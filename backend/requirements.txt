# ─── Core ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic[email]>=2.7.4,<3.0.0
pydantic-settings>=2.3.1
python-multipart>=0.0.9
python-dotenv>=1.0.1

# ─── Database ────────────────────────────────────────────────────────
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.1
psycopg2-binary>=2.9.9

# ─── Redis / Caching ─────────────────────────────────────────────────
redis>=5.0.4
celery>=5.4.0

# ─── LangChain / LLM ─────────────────────────────────────────────────
langchain>=0.2.5
langchain-core>=0.2.9
langchain-community>=0.2.5
langchain-groq>=0.1.6
langchain-google-genai>=1.0.6
langgraph>=0.2.0
langsmith>=0.1.77

# ─── RAG / Vector Store ───────────────────────────────────────────────
# Pinecone serverless (replaces ChromaDB — no Docker needed)
pinecone>=3.2.2
# fastembed: ONNX-based embeddings, no PyTorch needed (~80MB vs 2GB)
fastembed>=0.4.0
pypdf>=4.2.0
docx2txt>=0.8
tiktoken>=0.7.0

# ─── Auth ─────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ─── Utilities ────────────────────────────────────────────────────────
httpx>=0.27.0
aiofiles>=23.2.1
python-slugify>=8.0.4
rich>=13.7.1
loguru>=0.7.2

# ─── Phase 4 Task Queue (packages already declared above) ─────────────────────
# celery>=5.3.6          → declared in Redis/Caching section
# redis>=5.0.4           → declared in Redis/Caching section
# psycopg2-binary>=2.9.9 → declared in Database section
# These are used by tasks/celery_app.py and tasks/workflows.py
